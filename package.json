{
  "name": "sapui5-responsive-form",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "ui5 serve --open index.html",
    "build": "ui5 build --all",
    "postbuild": "copy dist\\index.html dist\\404.html",
    "deploy": "npm run build && npm run postbuild && gh-pages -d dist"
  },
  "devDependencies": {
    "@ui5/cli": "^3.0.0",
    "gh-pages": "^6.3.0"
  }
}
